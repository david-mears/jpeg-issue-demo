<h1>You are viewing the game called <%= @game.slug %></h1>
<h2>Slug: <%= @game.slug %></h2>
<h2>Players: <%= @game.players.count%></h2>
<h2>Cups:</h2>
<% @game.cups.each do |cup| %>
  <h3><%= cup.human_readable_name%></h3>
  <%= image_tag(cup.image, height: '100', alt: cup.human_readable_name) %>
<% end %>
